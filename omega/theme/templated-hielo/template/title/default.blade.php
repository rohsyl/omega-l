<h{{ isset($level['value']) ? $level['value'] : '1' }} class="title">{{ $title }}
    @if(!empty($subtitle))
        <small class="subtitle">{{ $subtitle }}</small>
    @endif

</h{{ isset($level['value']) ? $level['value'] : '1' }}>